#!/bin/bash

basedir=${0%/*}

image=$1
if [ -z "$image" ]; then
	echo "$0: expected image argument" >&2
	exit 1
fi

testbus-control start crash_host $image
$basedir/kdump
testbus-control stop crash_host

